version: 2
jobs:
  build:
    docker:
      - image: gcc:8.1.0
    working_directory: ~/work
    steps:
      - checkout
      - run: gcc giggle.c && giggle.out
      - store_artifacts:
          path: ~/work
          destination: /home/build
