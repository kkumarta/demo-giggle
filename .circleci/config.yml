version: 2
jobs:
  build:
    docker:
      - image: gcc:8.1.0
    working_directory: ~/work
    steps:
      - checkout
      - run: gcc giggle.c -o giggle.out
      - run: giggle.out
      - store_artifacts:
          path: /home/artifact
          destination: giggle.out
